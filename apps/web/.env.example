# Buttondown API key for the waitlist / updates capture (server-side only — never
# exposed to the client). Get it from Buttondown → Settings → Programming → API.
# Without it, the form returns a "briefly unavailable" message instead of signing
# anyone up. Set this in the Vercel project's environment variables.
BUTTONDOWN_API_KEY=
