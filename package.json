{
  "name": "bunsen",
  "version": "0.2.0",
  "private": true,
  "description": "A general-purpose experiment runner for agentic systems",
  "author": "Matthew Job Granmoe",
  "license": "SEE LICENSE IN LICENSE",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@9.1.1",
  "scripts": {
    "build": "pnpm -r build",
    "clean": "pnpm -r clean && rm -rf node_modules",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "gen:skill-reference": "node scripts/gen-skill-reference.mjs",
    "bn": "bun packages/cli/dist/bin.js"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "prettier": "^3.1.0",
    "typescript": "^5.3.0",
    "vitest": "^2.0.0"
  }
}
